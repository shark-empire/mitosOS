[package]
name = "libmitos"
version = "0.1.0"
edition = "2024"
authors = ["mitosOS Developers"]
description = "User-space system library for mitosOS"

[dependencies]
# Completely bare-metal, no external dependencies
